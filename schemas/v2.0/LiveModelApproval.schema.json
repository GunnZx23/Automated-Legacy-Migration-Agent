{
  "additionalProperties": false,
  "description": "Explicit operator gate for sending capstone context to a live provider.",
  "properties": {
    "allow_live_api": {
      "const": true,
      "title": "Allow Live Api",
      "type": "boolean"
    },
    "allow_prompt_data_sharing": {
      "const": true,
      "title": "Allow Prompt Data Sharing",
      "type": "boolean"
    },
    "approved_by": {
      "maxLength": 160,
      "minLength": 1,
      "title": "Approved By",
      "type": "string"
    },
    "approved_remote_provider_id": {
      "anyOf": [
        {
          "maxLength": 160,
          "minLength": 1,
          "pattern": "^[^\\x00\\r\\n]+$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Authenticated remote provider explicitly approved by the operator. Required by provider-managed CLI adapters before source is sent.",
      "title": "Approved Remote Provider Id"
    }
  },
  "required": [
    "allow_live_api",
    "allow_prompt_data_sharing",
    "approved_by"
  ],
  "title": "LiveModelApproval",
  "type": "object"
}
